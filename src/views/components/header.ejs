<div class="bg-[#85082b] py-1 sm:px-4 px-2">
  <div class="container mx-auto flex justify-between gap-5">
    <h1 class="capitalize font-medium text-white" id="date-display"></h1>
  </div>
</div>

<header class="py-4 sm:px-4 px-2 bg-[#f1f1f1]">
  <div class="flex justify-between gap-5 items-center container mx-auto">
    <img src="/images/segmite.webp" alt="Segmite Logo" class="sm:size-30 size-25" />
    <h2 class="lg:text-xl sm:text-lg font-semibold capitalize text-[#85082b] w-60">
      Society of Economic Geologists & Mineral Technologists
    </h2>
  </div>
</header>

<div nav-stop-propagation class="bg-[#85082b] shadow-md sticky z-50 top-0 left-0 py-2 sm:px-4 px-2">
  <nav class="container mx-auto flex items-center justify-between gap-5">
    <div
      class="size-10 p-2 rounded-sm bg-white shrink-0 cursor-pointer lg:hidden"
      onclick="isVisibleMobileMenu()"
    >
      <svg
        xmlns="http://www.w3.org/2000/svg"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"
        class="lucide lucide-list-chevrons-down-up-icon lucide-list-chevrons-down-up"
      >
        <path d="M3 5h8" />
        <path d="M3 12h8" />
        <path d="M3 19h8" />
        <path d="m15 5 3 3 3-3" />
        <path d="m15 19 3-3 3 3" />
      </svg>
    </div>
    <div class="flex gap-8 items-center container mx-auto max-lg:hidden uppercase text-white">
      <a class="nav-menu" href="/">Home</a>
      <a class="nav-menu" href="/about">About us</a>
      <a class="nav-menu" href="/ijeeg">IJEEG</a>
      <a class="nav-menu" href="/segmite-events">SEGMITE Events</a>
      <a class="nav-menu" href="/contact">Contact us</a>
    </div>
    <a
      class="px-4 py-2 rounded-sm bg-blue-900 hover:bg-blue-800 text-white font-semibold whitespace-nowrap"
      href="/membership-form"
      >SEGMITE Membership FORM</a
    >
  </nav>

  <!-- Mobile Menu -->
  <div
    id="mobile-menu-box"
    class="bg-white rounded-sm shadow-xl z-50 absolute left-0 top-full w-full grid overflow-hidden h-0 duration-500 transition-all ease-in-out"
  >
    <a href="/" class="py-2 px-4 border-b hover:bg-gray-100 font-medium text-[#333] border-[#ddd]"
      >Home</a
    >
    <a
      href="/about"
      class="py-2 px-4 border-b hover:bg-gray-100 font-medium text-[#333] border-[#ddd]"
      >About Us</a
    >
    <a
      href="/ijeeg"
      class="py-2 px-4 border-b hover:bg-gray-100 font-medium text-[#333] border-[#ddd]"
    >
      IJEEG</a
    >
    <a
      href="/segmite-events"
      class="py-2 px-4 border-b hover:bg-gray-100 font-medium text-[#333] border-[#ddd]"
      >SEGMITE Events</a
    >
    <a href="/contact" class="py-2 px-4 border-b hover:bg-gray-100 font-medium text-[#333] border-[#ddd]">
      Contact Us</a
    >
  </div>
</div>

<!-- ======================================= -->
<!-- Javascript -->
<!-- ======================================= -->
<script>
  let months = [
    "january",
    "february",
    "march",
    "april",
    "may",
    "june",
    "july",
    "august",
    "september",
    "october",
    "november",
    "december",
  ];

  const date = new Date();
  const format = `${months[date.getMonth()]} ${date.getDate()}, ${date.getFullYear()}`;
  document.getElementById("date-display").innerText = format;

  document.body.addEventListener("click", () => closeMobileMenu());

  // Mobile Menu Logic
  document
    .querySelector("[nav-stop-propagation]")
    .addEventListener("click", (e) => e.stopPropagation());
  const mobileMenuBox = document.getElementById("mobile-menu-box");
  function closeMobileMenu() {
    const style = window.getComputedStyle(mobileMenuBox);
    if (style.height != "0px") {
      mobileMenuBox.style.height = `0px`;
    }
  }
  function isVisibleMobileMenu() {
    const style = window.getComputedStyle(mobileMenuBox);
    if (style.height === "0px") {
      mobileMenuBox.style.height = `205px`;
    } else {
      mobileMenuBox.style.height = `0px`;
    }
  }
</script>
